#include <string>
using namespace std;

string reverseString(string str){
  for (int i = str.size -1; i >= (str.size()/2); --i){
    char k = str[i];
    str[i] = str[str.size() - 1 - i];
    str[str.size() - 1 - i] = k;
  }
  return str;
}
